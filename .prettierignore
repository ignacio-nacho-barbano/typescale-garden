.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example

# Ignore files for PNPM, NPM and YARN
pnpm-lock.yaml
package-lock.json
yarn.lock

# Turborepo
.turbo

# Compiled output (core/dist) — generated, and already gitignored
dist

# Captured golden fixtures: 200KB of machine-written JSON. Reformatting it to
# 100 columns on every `npm run format` is pure churn, and it is only ever
# regenerated wholesale.
core/src/__tests__/fixtures/golden.json
