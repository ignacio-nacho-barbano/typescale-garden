# Feature name

Shared typescales on plugin

## Description

Allow Figma plugin users to load the typescales they have saved directly without copying through a selector
Loading through the code should still be available, just not the default, there should be a button to show the text area in the extension
If possible, user session should be shared with the web app by default
